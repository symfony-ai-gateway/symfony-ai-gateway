{% extends "@AIGateway/dashboard/base.html.twig" %}

{% block title %}AIGateway Dashboard — Error{% endblock %}

{% block content %}
<div class="empty">
    <h3>Error</h3>
    <p>{{ message }}</p>
    <a href="{{ dash.turl('/dashboard', dashboard_token|default('')) }}" class="btn btn-primary" style="margin-top:16px">Back to Dashboard</a>
</div>
{% endblock %}
